const router = require('express').Router();
// /api/stalls
//router.route('/').get(getStalls).post(addStall).put(updateStall); 

///api/stalls/:stallId
//router.route('/:stallId').get(getStall).delete(deleteStall); 


module.exports = router;



//ToDo :deleting a stall will delete its associated stalls also
//stall owner/admin can crud stall details
